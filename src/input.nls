tsteps  <- 10
samples <- 10000000

pi <- 0.0
while(0<tsteps) {
    tsteps <- tsteps - 1
    x <- rand(samples)
    y <- rand(samples)
    m <- sum(sqrt(x^2 + y^2) < 1.0) * 4.0
    pi <- pi + m / samples
}

? pi
